import {Logo} from "./logo";import Link from "next/link";import {ArrowLeft} from "lucide-react";
export function AppShell({children,title,eyebrow,back="/"}:{children:React.ReactNode;title:string;eyebrow:string;back?:string}){return <main className="min-h-screen grid-noise"><header className="border-b border-white/10 bg-ink/80 backdrop-blur-xl"><div className="shell flex h-20 items-center justify-between"><Logo/><Link href={back} className="btn-secondary !px-4 !py-2"><ArrowLeft size={15}/> Retour</Link></div></header><div className="shell py-10"><p className="label text-violet">{eyebrow}</p><h1 className="text-3xl font-semibold tracking-[-.035em] sm:text-4xl">{title}</h1>{children}</div></main>}
